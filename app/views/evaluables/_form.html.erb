<%= form_with(model: evaluable) do |form| %>
  <% if evaluable.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(evaluable.errors.count, "error") %> prohibited this evaluable from being saved:</h2>

      <ul>
        <% evaluable.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
