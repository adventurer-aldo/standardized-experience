{
    "name": "convertible",
    "private": true,
    "dependencies": {
        "@hotwired/turbo-rails": "^7.1.1",
        "esbuild": "^0.14.25",
        "react": "^17.0.2",
        "react-dom": "^17.0.2",
        "reactjs-popup": "^2.0.5"
    },
    "scripts": {
        "build": "esbuild --bundle app/build_file.js --outfile=app/javascript/application.js --allow-overwrite",
        "watch": "esbuild --watch --bundle app/build_file.js --outfile=app/javascript/application.js --allow-overwrite"
    }
}
